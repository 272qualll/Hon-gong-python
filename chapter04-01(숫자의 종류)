#type of numbers
list1 = [1,2,3,4,1,2,3,1,4,1,2,3]
t_num = {}

for k in list1:
    if k not in t_num:
        t_num[k] = 0
    t_num[k] += 1
    
print("There are {} types of numbers used in {}.".format(len(t_num), list1))
print(f"Reference : {t_num}")
